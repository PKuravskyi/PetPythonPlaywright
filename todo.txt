To add:

Logger, for easier debugging
More tests (UI + API + Visual comparison)
Files handling (json, csv etc.)
AI support (?)
Try to download only necessary browsers for CI
Docstrings for everything that is not obvious
